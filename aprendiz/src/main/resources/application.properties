#spring.application.name=aprendiz
#server.port=8080

#Configuraci�n para la base de datos
#spring.datasource.url=jdbc:mysql://localhost:3306/api_aprendiz
#spring.datasource.username=root
#spring.datasource.password=

# Importar variables de entorno desde .env (aseg�rate que est� en la ra�z del proyecto)
#spring.config.import=optional:file:.env[.properties],optional:file:../.env[.properties]

#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/api_aprendiz?useSSL=false&serverTimezone=UTC}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}

#variables de entorno para el despliegue
spring.datasource.url = jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Configuraci�n hibernate
spring.jpa.show-sql:true
spring.jpa.hibernate.ddl-auto:update

#Configuraci�n para la documentaci�n con Swagger
springdoc.swagger-ui.path=/swagger-ui-custom.html